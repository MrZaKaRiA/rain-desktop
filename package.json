{
  "name": "rain-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "RAIN Desktop — a configurable native launcher for your store (Tauri v2)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "icons": "tauri icon app-icon.png",
    "set-url": "node scripts/set-default-url.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
